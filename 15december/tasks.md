# 🚀 Практический Урок: Класс `java.util.LinkedList`

## 🎯 Цели Урока

Понять внутреннее устройство класса `LinkedList`, освоить его уникальные методы и научиться выбирать его в качестве оптимальной структуры данных для решения конкретных задач.

-----

## 1\. Понимание Структуры и Отличий (Теория и Сравнение)

Для эффективного использования `LinkedList` необходимо четко понимать, как он работает "под капотом" и чем отличается от `ArrayList`.

### Задание

1.  **Сравнительный Анализ:** Составьте **таблицу сравнения** между **`ArrayList`** и **`LinkedList`** по следующим критериям. Укажите временную сложность (Big O Notation, например, $O(1)$, $O(n)$) для каждой операции.

| Операция | `ArrayList` (временная сложность) | `LinkedList` (временная сложность) |
| :--- | :--- | :--- |
| **Получение по индексу** (`get(index)`) | $O(\dots)$ | $O(\dots)$ |
| **Вставка в начало** (`add(0, element)`) | $O(\dots)$ | $O(\dots)$ |
| **Вставка в конец** (`add(element)`) | $O(\dots)$ | $O(\dots)$ |
| **Удаление в середине** (`remove(index)`) | $O(\dots)$ | $O(\dots)$ |
| **Базовая структура данных** | (Укажите) | (Укажите) |

2.  **Сценарии Применения:** Ответьте на вопрос:
    > "Предположим, вы создаете систему, где требуется часто **вставлять и удалять** элементы из **начала** списка (например, логи последних действий). Какую коллекцию вы выберете и **почему**?"

-----

## 2\. Практика Основных Методов (Deque и Queue)

Поскольку `LinkedList` реализует интерфейсы **`Deque`** (двусторонняя очередь) и **`Queue`** (очередь), он предоставляет множество специализированных методов. Освоим их\!

### Задание

Создайте класс с методом `main` и выполните следующие шаги:

1.  **Инициализация:** Создайте экземпляр **`LinkedList<String>`** с названием `planets`. Добавьте в него 3-4 планеты (`"Меркурий"`, `"Венера"`, `"Земля"`, `"Марс"`).
2.  **Двусторонняя Очередь (Deque):**
      * Используйте метод **`addFirst()`** для добавления `"Солнце"` в самое начало списка.
      * Используйте метод **`removeLast()`** для удаления последней планеты из списка.
3.  **Изучение без Удаления (Peek):**
      * Используйте метод **`peekFirst()`** для получения, но **не удаления**, первого элемента и выведите его.
      * Убедитесь, что после `peekFirst()` размер списка не изменился.
4.  **Обратный Обход:**
      * Получите **обратный итератор** с помощью метода **`descendingIterator()`**.
      * Используйте этот итератор для обхода и вывода **всех элементов списка в обратном порядке**.

-----

## 3\. Задачи на Алгоритмы (Усложненное)

Эти задачи требуют манипуляций с элементами и их порядком, что позволяет закрепить понимание структуры.

### Задание

Напишите отдельные статические методы для решения следующих задач.

### Задача 3.1: Переворот Списка

Напишите обобщенный статический метод:

```java
public static <T> void reverseList(LinkedList<T> list)
```

Метод должен **перевернуть порядок** элементов в переданном списке `list`.

  * **Подсказка:** Подумайте, как вы можете использовать методы **`addFirst()`** и **`pollFirst()`** для эффективного переворота.

### Задача 3.2: Фильтрация Дубликатов

Напишите статический метод:

```java
public static void removeDuplicates(LinkedList<Integer> list)
```

Метод должен удалить из списка **все дубликаты**, оставив только **первое вхождение** каждого числа.

  * **Подсказка:** Для отслеживания уже встреченных элементов можно использовать коллекцию **`HashSet`**.

### Задача 3.3: Использование как Стек/Очередь (Бонус)

Используйте `LinkedList<String>` для реализации **простой системы отката/повтора действий** (например, в текстовом редакторе).

1.  Создайте `LinkedList` как **"Стек Истории"**.
2.  Используйте метод **`push()`** для записи нового действия (например, `"Напечатана буква A"`).
3.  Используйте метод **`pop()`** для **отката** (удаления) последнего действия.
4.  Выведите сообщение, объясняющее, как методы **`push/pop`** класса `LinkedList` реализуют поведение **Стека (LIFO)**.
