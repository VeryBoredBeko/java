## 🟢 Задачи легкой сложности (3)

### **Задача 1. Страны и столицы**

Создайте `HashMap<String, String>`, где ключ — название страны, значение — столица.
Добавьте не менее 5 элементов и выведите все пары «страна – столица» на экран.

**Требования:**

* Использовать `put()`
* Использовать `entrySet()` для вывода

---

### **Задача 2. Оценки студентов**

Создайте `HashMap<String, Integer>`, где ключ — имя студента, значение — его оценка.
Выведите оценку конкретного студента по имени.

**Требования:**

* Использовать `get()`
* Проверить наличие ключа с помощью `containsKey()`

---

### **Задача 3. Подсчёт элементов**

Создайте `HashMap<String, Integer>` и добавьте несколько пар «товар – количество».
Выведите общее количество записей в Map и проверьте, пуста ли коллекция.

**Требования:**

* Использовать `size()`
* Использовать `isEmpty()`

---

## 🟡 Задачи средней сложности (2)

### **Задача 4. Телефонная книга**

Реализуйте консольную телефонную книгу с использованием `HashMap<String, String>`
(имя — номер телефона).

**Функциональность:**

* Добавить контакт
* Найти номер по имени
* Удалить контакт

**Требования:**

* Использовать `put()`, `get()`, `remove()`
* Использовать `containsKey()` для проверки существования контакта

---

### **Задача 5. Подсчёт частоты слов**

Дана строка текста. Необходимо подсчитать, сколько раз каждое слово встречается в тексте, и сохранить результат в `HashMap<String, Integer>`.

**Требования:**

* Использовать `split()` для разбиения строки
* Использовать `put()` и `get()` (или `getOrDefault()`)
* Вывести результат через `entrySet()`

---

## 🔴 Задача повышенной сложности (1)

### **Задача 6. Учёт пользователей системы**

Реализуйте систему учёта пользователей, используя
`HashMap<String, User>`, где ключ — логин пользователя, значение — объект класса `User`.

**Класс `User` содержит:**

* `String name`
* `String email`
* `int age`

**Функциональность программы:**

* Добавление пользователя
* Поиск пользователя по логину
* Вывод всех пользователей
* Удаление пользователя

**Дополнительные требования:**

* Проверка уникальности логина
* Использование `entrySet()` при выводе
* Корректная работа с объектами в качестве значений Map
