# **Наследование, Generics, Ссылочные типы и Клонирование — Теория**

## 📌 1. Наследование (Inheritance)

**Наследование** — механизм ООП, позволяющий одному классу получать поля и методы другого.

### **Зачем нужно**

* Повторное использование кода
* Создание иерархий
* Реализация полиморфизма

### **Синтаксис**

```java
class Parent { }
class Child extends Parent { }
```

### **Ключевые моменты**

* Дочерний класс расширяет поведение родительского.
* Переопределение методов выполняется через `@Override`.
* Конструкторы вызываются снизу вверх: всегда сначала `super()`.

---

## 📌 2. Обобщения (Generics)

**Generics** позволяют создавать классы и методы, работающие с разными типами данных.

### **Для чего нужны**

* Безопасность типов (нет `ClassCastException`)
* Улучшение читаемости
* Повторное использование логики

### **Пример**

```java
class Box<T> {
    T value;
}
```

### **Ограничения Generic-параметров**

```java
class NumberBox<T extends Number> { }
```

---

## 📌 3. Ссылочные типы в Java

В Java переменные объектов хранят **ссылки**, а не сами данные.
При передаче объекта в метод передаётся **копия ссылки**, но оба указателя ведут к одному объекту.

### 🔍 Пример изменения объекта внутри метода

```java
void change(User u) {
    u.name = "New";
}
```

После вызова имя в оригинальном объекте тоже изменится.

### ✔ Переназначение ссылки — не влияет

```java
void reassign(User u) {
    u = new User();
}
```

Оригинальный объект остаётся прежним.

---

## 📌 4. Клонирование объектов

Используется для создания копий объектов.

---

### **4.1. Поверхностное копирование (Shallow copy)**

* Копируются только **значения полей**.
* Если поле — объект, копируется **ссылка**, а не сам объект.

```java
class User implements Cloneable {
    @Override
    protected Object clone() throws CloneNotSupportedException {
        return super.clone();
    }
}
```

---

### **4.2. Глубокое копирование (Deep copy)**

* Создаётся **полная независимая копия** всех вложенных объектов.

```java
@Override
protected Object clone() throws CloneNotSupportedException {
    User cloned = (User) super.clone();
    cloned.profile = new Profile(cloned.profile.bio);
    return cloned;
}
```

---

## 📌 5. Отличия Shallow vs Deep Copy

| Характеристика | Shallow    | Deep                    |
| -------------- | ---------- | ----------------------- |
| Примитивы      | копируются | копируются              |
| Ссылки         | копируются | создаются новые объекты |
| Независимы?    | ❌ Нет      | ✔ Да                    |
| Используется   | быстро     | безопасно               |

---

## ## 📚 Короткий итог

* **Наследование** — расширение классов и переопределение методов.
* **Generics** — универсальные классы без потери типизации.
* **Ссылочные типы** — переменная хранит ссылку, а не объект.
* **Shallow Copy** — копия, разделяющая вложенные объекты.
* **Deep Copy** — полностью независимая копия.
