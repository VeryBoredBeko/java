# 🎯 Техническое Задание: Консольный Менеджер Задач v1.0

## 🚀 Ваша задача (Цель проекта)

Вам нужно создать простую программу для управления задачами. Главное здесь — научиться правильно выбирать и использовать инструменты (коллекции Java) для разных целей, чтобы ваша программа работала логично и быстро.

## 🛠️ Ваши инструменты (Коллекции Java)

Вы будете использовать две "коробки" для хранения информации:

1. **Основная очередь задач (Название: `pendingTasks`)**
* **Что это:** Это ваша главная рабочая очередь.
* **Инструмент:** **`ArrayDeque<String>`**
* **Почему:** Этот инструмент позволяет очень быстро добавлять или брать элементы как с начала, так и с конца. Вы будете использовать его как для **срочных** задач (добавляем в начало), так и для **обычных** (добавляем в конец).


2. **Архив выполненных задач (Название: `completedArchive`)**
* **Что это:** Список завершенных дел, которые вы не будете часто менять, только добавлять в конец и иногда просматривать.
* **Инструмент:** **`ArrayList<String>`**
* **Почему:** Он отлично подходит для хранения большого списка данных, когда вам нужно быстро просмотреть их по номеру (индексу).



## 📋 Требования к функциям (Меню программы)

Ваша программа должна работать в цикле, постоянно показывая меню и ожидая выбора пользователя.

| Номер | Что делает функция | Как это работает (Логика) | Какой метод `ArrayDeque` вы используете |
| --- | --- | --- | --- |
| **1** | **Добавить Срочную задачу** | Новая задача должна стать **первой** в очереди. | `push(String)` |
| **2** | **Добавить Обычную задачу** | Новая задача добавляется **в конец** очереди. | `offer(String)` |
| **3** | **Выполнить задачу** | Удалить **первую** задачу из очереди (`pendingTasks`) и **перенести** её в `completedArchive`. | `poll()` |
| **4** | **Посмотреть следующую** | Показать, какая задача будет выполнена следующей, но **НЕ удалять** её. | `peek()` |
| **5** | **Показать все текущие** | Вывести на экран весь список задач, которые ждут выполнения. | Итерация по `ArrayDeque` |
| **6** | **Показать Архив** | Вывести весь список выполненных задач. | Итерация по `ArrayList` |
| **0** | **Выход** | Завершить работу программы. | - |

## 💡 Полезные советы (Tips & Tricks)

1. **Ввод данных:** Используйте `Scanner` для получения выбора из меню и текста задач.
2. **Обработка выбора:** Используйте конструкцию `switch` для обработки цифр от 0 до 6, которые вводит пользователь.
3. **Защита от ошибок:** Что произойдет, если пользователь попробует "Выполнить задачу" (пункт 3), а очередь пуста? Методы `poll()` и `peek()` вернут вам `null`. Используйте проверку на `null`, чтобы вывести вежливое сообщение, а не сломать программу!
4. **Служебные методы:** Не забывайте про метод `isEmpty()` для проверки пуста ли ваша очередь.

## ✅ Что будет оцениваться

* **Логика (40%):** Каждая функция меню работает так, как описано выше (например, срочная задача действительно идет в начало).
* **Правильный инструмент (30%):** Вы используете именно те методы (`push`, `poll`, `offer`, `peek`), которые необходимы для эффективной работы `ArrayDeque`.
* **Понятность кода (20%):** Ваш код легко читается, у переменных понятные имена (`pendingTasks`, а не `a`).
* **Дружелюбность (10%):** Меню в консоли красивое и понятное для пользователя.

**Удачи! Покажите, что вы умеете выбирать правильный инструмент для правильной работы.**
