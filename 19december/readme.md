# 📚 Очереди (Queues) и класс ArrayDeque

## 🚀 Введение

**Очередь (Queue)** — это коллекция, разработанная для хранения элементов перед их обработкой. Очереди, как правило, упорядочивают элементы по принципу **FIFO** (First-In, First-Out, «первым пришел — первым ушел»).

Интерфейс `Queue` расширяет интерфейс `Collection` и предоставляет методы для вставки, извлечения и проверки элементов.

## 🧐 Интерфейс `Queue` (Основные Методы)

| Категория | Выбрасывает исключение | Возвращает специальное значение | Описание |
| --- | --- | --- | --- |
| **Вставка** | `add(e)` | `offer(e)` | Вставляет элемент в очередь. |
| **Удаление** | `remove()` | `poll()` | Удаляет и возвращает элемент из начала очереди. |
| **Проверка** | `element()` | `peek()` | Возвращает элемент из начала очереди, не удаляя его. |

---

## 🛠️ ArrayDeque

Класс **`ArrayDeque`** реализует интерфейс `Deque` (Double Ended Queue — двусторонняя очередь), который, в свою очередь, расширяет интерфейс `Queue`.

### 💡 Особенности ArrayDeque:

* **Динамический массив:** Реализован на основе динамического массива.
* **Двусторонность:** Может использоваться как обычная **Очередь** (FIFO) или как **Стек** (LIFO - Last-In, First-Out).
* **Быстродействие:** Обеспечивает почти константное время O(1) для операций вставки и удаления как с начала, так и с конца.
* **Отсутствие `null`:** Элементы не могут быть `null`.
* **Несинхронизирован:** Не подходит для многопоточной среды без внешней синхронизации.

## 🧑‍💻 Примеры использования `ArrayDeque`

### 1. Использование как Очереди (Queue - FIFO)

Используем методы из интерфейса `Queue`: `offer()`, `poll()`, `peek()`.

```java
import java.util.ArrayDeque;
import java.util.Queue;

public class QueueExample {
    public static void main(String[] args) {
        // Создание ArrayDeque, который будет использоваться как Queue
        Queue<String> tasks = new ArrayDeque<>();

        // 1. offer(e): Добавление элементов (аналог add())
        tasks.offer("Убрать комнату");
        tasks.offer("Позвонить маме");
        tasks.offer("Сделать отчет");

        System.out.println("Очередь задач: " + tasks); // [Убрать комнату, Позвонить маме, Сделать отчет]

        // 2. peek(): Просмотр элемента в начале очереди (первая задача)
        String nextTask = tasks.peek();
        System.out.println("Следующая задача (peek): " + nextTask); // Убрать комнату
        System.out.println("Очередь после peek: " + tasks); // [Убрать комнату, Позвонить маме, Сделать отчет]

        // 3. poll(): Извлечение (удаление) элемента из начала очереди
        String completedTask = tasks.poll();
        System.out.println("Завершена задача (poll): " + completedTask); // Убрать комнату
        
        String nextCompletedTask = tasks.poll();
        System.out.println("Завершена задача (poll): " + nextCompletedTask); // Позвонить маме

        System.out.println("Оставшаяся очередь: " + tasks); // [Сделать отчет]
        
        // 4. poll() на пустой очереди
        tasks.poll();
        String result = tasks.poll(); // null
        System.out.println("poll() на пустой очереди: " + result); 
    }
}

```

### 2. Использование как Стека (Stack - LIFO)

Используем методы из интерфейса `Deque`: `push()`, `pop()`, `peek()`.

```java
import java.util.ArrayDeque;
import java.util.Deque;

public class StackExample {
    public static void main(String[] args) {
        // Создание ArrayDeque, который будет использоваться как Stack
        Deque<Integer> history = new ArrayDeque<>();

        // 1. push(e): Добавление элемента в начало (вершину) стека
        history.push(101); // Текущая страница
        history.push(102); // Предыдущая страница
        history.push(103); // Еще предыдущая

        System.out.println("История страниц (стек): " + history); // [103, 102, 101]

        // 2. peek(): Просмотр элемента на вершине стека
        int currentPage = history.peek();
        System.out.println("Текущая страница (peek): " + currentPage); // 103

        // 3. pop(): Извлечение (удаление) элемента с вершины стека
        int backPage = history.pop();
        System.out.println("Переход назад (pop): " + backPage); // 103
        
        backPage = history.pop();
        System.out.println("Переход назад (pop): " + backPage); // 102

        System.out.println("Оставшаяся история: " + history); // [101]
    }
}

```

---

## 🆚 ArrayDeque vs LinkedList

Хотя `LinkedList` также реализует `Deque`, `ArrayDeque` обычно предпочтительнее для использования в качестве очереди или стека:

| Характеристика | ArrayDeque | LinkedList |
| --- | --- | --- |
| **Реализация** | Динамический массив | Двусвязный список |
| **Производительность** | Быстрее для операций с начала/конца O(1) | O(1) для начала/конца, но имеет накладные расходы на работу с узлами. |
| **Использование памяти** | Более компактно, нет затрат на хранение ссылок на узлы. | Требует больше памяти для хранения ссылок (`prev` и `next`) для каждого элемента. |

В общем, **`ArrayDeque`** является более эффективным выбором для большинства случаев использования очередей и стеков.

Могу ли я помочь вам с другими примерами кода или углубиться в разницу между `Queue` и `Deque`?
