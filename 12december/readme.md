# **ArrayList и интерфейс List — Практический модуль**

## 📘 Краткая теория

В Java интерфейс **`List`** представляет упорядоченную коллекцию элементов, где каждый элемент имеет свой **индекс**. Основные особенности `List`:

* поддерживает дублирующиеся элементы;
* сохраняет порядок вставки;
* позволяет управлять элементами по индексам.

Наиболее часто используемая реализация — **`ArrayList`**, динамический массив, который:

* автоматически расширяется при добавлении элементов;
* обеспечивает **быстрый доступ по индексу (O(1))**;
* менее эффективен при вставках/удалениях из середины списка;
* предоставляет удобные методы управления коллекцией
  (`add`, `remove`, `get`, `set`, `contains`, `size` и др.).

`ArrayList` является частью **Java Collections Framework** и лежит в пакете `java.util`.

---

## 🧭 Структура практической работы

Вам предстоит выполнить набор задач, сгруппированных по смысловым блокам.
Задания помогут освоить:

* базовые операции со списками,
* работу с индексами,
* модификацию элементов и структуры,
* сортировку и перестановку,
* объединение и сравнение списков,
* управление ёмкостью `ArrayList`.

---

## 📄 Перейти к заданиям

👉 **[Смотреть задачи — tasks.md](./tasks.md)**
