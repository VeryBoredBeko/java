## 🟢 ЛЁГКИЙ УРОВЕНЬ (8 задач)

### **1. Сортировка чисел**

Создайте список `List<Integer>` и отсортируйте его по возрастанию с помощью `Collections.sort()`.

---

### **2. Сортировка строк**

Создайте список строк и выполните сортировку:

* по алфавиту
* в обратном порядке

---

### **3. Класс Person (Comparable)**

Создайте класс `Person` с полем `age`.
Реализуйте интерфейс `Comparable`, чтобы сортировка происходила по возрасту.

---

### **4. Сортировка объектов Person**

Создайте список из нескольких объектов `Person` и отсортируйте его.

---

### **5. Comparator по имени**

Создайте `Comparator<Person>`, который сортирует людей по имени.

---

### **6. Comparator по длине строки**

Отсортируйте список строк по длине строки, используя `Comparator`.

---

### **7. Лямбда-сортировка**

Отсортируйте список чисел по убыванию, используя лямбда-выражение.

---

### **8. Collections.sort с Comparator**

Используйте `Collections.sort(list, comparator)` для сортировки списка объектов.

---

## 🟡 СРЕДНИЙ УРОВЕНЬ (5 задач)

### **1. Класс Student (Comparable)**

Создайте класс `Student` с полями:

* `name`
* `grade`

Реализуйте `Comparable`, сортировка по `grade`.

---

### **2. Несколько Comparator**

Создайте `Comparator<Student>`:

* по имени
* по имени в обратном порядке

---

### **3. Сортировка по двум полям**

Отсортируйте студентов:

* сначала по оценке
* при равенстве — по имени

---

### **4. Comparator.comparing**

Используйте:

```java
Comparator.comparing(Student::getGrade)
```

для сортировки списка студентов.

---

### **5. Изменение порядка сортировки**

Измените существующий Comparator так, чтобы сортировка была по убыванию (`reversed()`).

---

## 🔴 СЛОЖНЫЙ УРОВЕНЬ (5 задач)

### **1. Comparable vs Comparator (анализ)**

Объясните, почему **Comparator предпочтительнее**, если нужно несколько вариантов сортировки.

---

### **2. Класс Product**

Создайте класс `Product`:

* `name`
* `price`
* `rating`

Реализуйте:

* `Comparable` — по цене
* `Comparator` — по рейтингу

---

### **3. Комбинированная сортировка**

Отсортируйте `Product`:

* по рейтингу (убывание)
* затем по цене (возрастание)

---

### **4. Сортировка с Optional**

Отсортируйте список объектов, у которых поле `name` может быть `null`.
`null`-значения должны быть в конце списка.

---

### **5. Реальный кейс**

Дан список сотрудников `Employee`:

* `name`
* `salary`
* `experience`
